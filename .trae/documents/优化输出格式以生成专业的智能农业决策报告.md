我将修改 `client/mcp-client/client.py`，通过以下两步来提升建议的专业性和信息完整度：

1.  **更新 `SYSTEM_PROMPT`**：
    *   在提示词末尾添加**“标准回复格式”**章节，强制模型按照 Markdown 格式输出。
    *   要求输出包含三个核心板块：**环境监测实况**（天气、传感器、设备）、**作物生长分析**（作物类型、需水特性）、**智能决策建议**（结论、理由、后续指引）。

2.  **更新 `_rule_based_irrigation_advice` 函数**：
    *   重写该函数的返回值逻辑，使其在模型未能生成回答（Fallback 情况）时，也能输出同样格式的结构化报告。
    *   确保 Fallback 报告中包含具体的传感器数值（土壤湿度、温度等）和基于规则的明确建议。

**修改后的预期效果：**
无论是由 AI 模型直接生成，还是触发规则引擎兜底，您得到的回复都将是一份结构清晰、数据详实的**“智能农业决策报告”**，包含所有您要求的要素，像一位真正的农业专家在为您服务。