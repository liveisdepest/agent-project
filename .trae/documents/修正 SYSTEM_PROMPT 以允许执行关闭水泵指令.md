我将修改 `client/mcp-client/client.py` 中的 `SYSTEM_PROMPT`，明确授权 AI 可以执行“关闭水泵”的操作。

**计划修改点：**

1.  **修改 `SYSTEM_PROMPT` 的第四步（执行行动）**：
    *   将规则从“只有当用户明确说‘打开水泵’时……”改为更全面的表述。
    *   明确允许用户发送“关闭水泵”、“停止灌溉”等指令，并指示 AI 调用 `irrigation.control_pump(turn_on=False)`。
    *   保留“禁止自动开启”的安全限制，但放宽对“关闭”操作的限制，确保用户可以随时停止水泵。

**修改后的逻辑：**
当用户输入“关闭水泵”时，AI 将不再拒绝，而是会识别该意图并调用工具关闭水泵（同样会经过我们在代码中设置的二次确认拦截，确保操作意图明确）。